#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
/**
 * abs - Check for a integer
 * @r: The character to be checked
 *  Return: the absolute value of an integer.
 */
int _abs(int r)
{
	return (abs(r));
}
